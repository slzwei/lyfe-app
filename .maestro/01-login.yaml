# Flow: Login via OTP as the default mock manager (+6580000003).
# Thin compat shim for older runFlow references — prefer helpers/login-as.yaml directly.
appId: com.shawnlee.lyfe

---

- runFlow:
    file: helpers/login-as.yaml
    env:
      phone: "80000003"
