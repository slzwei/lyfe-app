# Flow: Admin logs in and lands on dashboard with admin-only tabs visible.
appId: com.shawnlee.lyfe

---

- runFlow:
    file: helpers/login-as.yaml
    env:
      phone: "80000001"

- assertVisible:
    id: "tab-team"
- assertVisible:
    id: "tab-events"
