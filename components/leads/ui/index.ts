/**
 * Leads-scoped UI primitives (mktr-leads UI/UX adoption · Option B).
 * Import design from here inside the leads module — never edit shared components.
 * Theme hooks/tokens live in `@/lib/leads/theme`.
 */
export { Txt, Eyebrow, type TxtProps } from './Txt';
export { Monogram } from './Monogram';
export { StatusChip } from './StatusChip';
export { SourceBadge } from './SourceBadge';
export { LivePill, LeadCardSkeleton, LeadsEmptyState, CompactEmpty } from './Skeletons';
